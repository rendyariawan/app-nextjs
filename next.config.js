/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["lab.dima-dan-rendy.shop", "app.dima-dan-rendy.shop", "bakeuda.pangkalpinangkota.go.id"],
  },
};

module.exports = nextConfig
